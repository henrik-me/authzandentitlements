{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Keycloak": {
    "_comment": "OIDC settings for the bank-web confidential client. The AppHost injects real values at runtime (do not hardcode a port). Authority takes precedence; otherwise AuthServerUrl + Realm form the realm URL.",
    "Authority": "",
    "AuthServerUrl": "",
    "Realm": "authz-bank",
    "ClientSecret": "bank-web-secret"
  }
}
